# Speedrift Decks

- `speedrift-ecosystem-story.html`: problem/solution narrative for why the ecosystem exists, including architecture and workflow graphics.

Live URL:

- `https://dbmcco.github.io/speedrift-ecosystem/decks/speedrift-ecosystem-story.html`
- Use the live URL for interactive navigation; GitHub file preview does not run deck JavaScript.

Open directly in a browser:

```bash
open docs/decks/speedrift-ecosystem-story.html
```
