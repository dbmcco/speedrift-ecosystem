# Speedrift Decks

- `speedrift-ecosystem-story.html`: problem/solution narrative for why the ecosystem exists, including architecture and workflow graphics.

Open locally from repo root:

```bash
open docs/decks/speedrift-ecosystem-story.html
```
